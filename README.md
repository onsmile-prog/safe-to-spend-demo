# Safe to Spend — free demo

The complete ADHD budget planner, running in your browser. Everything works;
nothing you type is saved, so reload and the example comes back.

Open it: the GitHub Pages link for this repository.

Built from the master file with `design/build-demo.ps1` — do not edit
`index.html` by hand.
